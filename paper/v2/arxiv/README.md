# FinCompass v2.0 arXiv source

Primary file: `main.tex`.

The bibliography database is included as `refs.bib`. A generated `main.bbl` is also included so the source can compile in environments where BibTeX is not invoked automatically.
